# Vercel 构建会读此变量生成反代（见 vercel.ts）；每个客户填自己的后台根地址，不要末尾 /
# VITE_API_BASE_URL=https://api.your-customer.com
#
# 若希望「仅构建反代用」与前端打包变量分开，可在 Vercel 只设：
# API_BACKEND_ORIGIN=https://api.your-customer.com
#
# 确需浏览器直连跨域 API 且后端已 CORS 时：
# VITE_API_CROSS_ORIGIN=true
#
# 若前端部署在自有域名（如静态 OSS/CDN）且该域名下没有配置 /api 反代到后台，
# 必须设 VITE_API_CROSS_ORIGIN=true，否则浏览器会请求「当前页同源 /api」并得到 404。
